---
- name: Ensure old versions of Docker are not installed.
  package:
    name:
      - docker
      - docker-common
      - docker-engine
    state: absent

# Fedora provides podman in main repos; no 3rd-party Docker CE repo required.
