---
# Skipping removal of existing Docker packages — user requested Docker remain installed.
# Previously this role removed Docker packages to avoid conflicts with podman-docker.
# If you want automatic migration in future, add a controlled migration task.

# Fedora provides podman in main repos; no 3rd-party Docker CE repo required.
